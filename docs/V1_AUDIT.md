# V1 Roadmap Audit

Date: 2026-06-20

Scope: concise audit of MVP 1 roadmap items currently marked done, checked against code and tests. This does not start new product work.

## Marked-Done Items

| Roadmap item | Marked done? | Evidence file/test | Mismatch or manual verification needed | Recommended next action |
| --- | --- | --- | --- | --- |
| Flutter project structure, mobile app shell, five-tab bottom navigation | Yes | `lib/main.dart`, `lib/widgets/bottom_nav_shell.dart`, `test/widget_test.dart` app smoke test | Wider responsive/sidebar work remains separately unchecked | Keep as done |
| Header with selected calendar name | Yes | `lib/widgets/life_shuffle_header.dart`, `test/widget_test.dart` header calendar-name test | Roadmap wording also says "calendar switcher"; no real switcher exists yet | Split/clarify roadmap item or leave switcher tracked by the separate unchecked switcher item |
| Keep Export, Publish, Check-in, Calendar switcher, AI out of main bottom nav | Yes | `lib/widgets/bottom_nav_shell.dart` | Manual UX check still useful on mobile viewport | Keep as done |
| Firebase setup, Google sign-in, basic auth gate | Yes | `lib/firebase_options.dart`, `lib/services/auth_service.dart`, `lib/screens/sign_in_screen.dart`, `lib/widgets/auth_gate.dart` | Needs periodic deployed sign-in smoke test after API key/domain changes | Keep as done |
| Short onboarding/setup flow, welcome explanation | Yes | `lib/screens/onboarding_screen.dart`, `lib/widgets/auth_gate.dart`, onboarding routing tests in `test/widget_test.dart` | Display/calendar prompts and intro completion now wait for initial Firestore sync; reduced-motion work remains unchecked | Keep as done |
| Confirm/edit display name after Google sign-in | Yes | `lib/screens/display_name_screen.dart`, `lib/state/app_state.dart`, `lib/services/persistence_service.dart`, `test/widget_test.dart` display-name and remote-sync tests | Firestore diagnostic now verifies safe booleans only | Keep as done |
| Calendar data model with title/name and first-calendar naming prompt | Yes | `lib/screens/calendar_name_screen.dart`, `lib/state/app_state.dart`, `lib/services/firestore_sync_service.dart`, onboarding routing tests | Calendar naming is one-time per Firestore calendar; clearing browser storage should not reset it | Keep as done |
| Structured Settings plus Account, Calendar rename/display, Planning style, Activity defaults, Publishing, Privacy/help | Yes | `lib/screens/settings_screen.dart`, `test/widget_test.dart` Settings tests | Current calendar can be renamed; switcher/members/roles/leave/delete remain unchecked | Keep as done |
| Activity, rule, planned item, and check-in status models | Yes | `lib/models/activity.dart`, `lib/models/day_plan.dart`, `lib/models/mock_data.dart`, `test/widget_test.dart` planner/check-in tests | Current storage is still flat selected-calendar state, not multi-calendar subcollections | Keep as done for one-calendar MVP foundation |
| Optional planning dimension settings and defaults | Yes | `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, `lib/screens/onboarding_screen.dart`, `test/widget_test.dart` dimension tests | Planning-dimensions onboarding step now surfaces the same toggles during setup | Keep as done |
| Starter activity library, categories, limited picker, custom activities, starter metadata/rules | Yes | `lib/services/starter_activity_library.dart`, `lib/screens/activities_screen.dart`, `test/widget_test.dart` starter/activity tests | Manual UX check recommended for starter picker on small mobile screens | Keep as done |
| First plan style choice | Yes | `lib/services/planner_service.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart` | Week start/time window/default activity count settings remain unchecked | Keep as done |
| Activity creation/editing, category/colour/icon, enabled state, difficulty/energy/social fields | Yes | `lib/screens/activities_screen.dart`, `lib/models/activity.dart`, activity form tests | Disabled dimensions are hidden from forms/cards; planner/export handling remains tracked as incomplete | Keep as done |
| Allowed day/time, max-per-week, no-consecutive-days, difficulty-aware planner rules | Yes | `lib/services/planner_service.dart`, planner tests in `test/widget_test.dart` | Manual testing still useful for confusing rule combinations | Keep as done |
| 7-day agenda generator and agenda-first plan view | Yes | `lib/services/planner_service.dart`, `lib/screens/plan_screen.dart`, planner/plan tests | No full day/month/year views, which are later direction | Keep as done |
| Today/Home screen and helpful empty states | Yes | `lib/screens/today_screen.dart`, `lib/screens/activities_screen.dart`, `lib/screens/plan_screen.dart`, `lib/screens/progress_screen.dart` | No-calendar, offline, and sync-problem empty states remain partially unfinished per roadmap note | Keep done items, leave unfinished states tracked |
| Lock/unlock, regeneration undo, regenerate unlocked only, generation conflict messages | Yes | `lib/state/app_state.dart`, `lib/screens/plan_screen.dart`, planner/regeneration tests | Shared-edit/sync conflict messages remain unchecked | Keep as done |
| Skippable app-open check-in prompt, quick catch-up, day sheet check-in, one-by-one review, week review | Yes | `lib/screens/today_screen.dart`, `lib/screens/check_in_catchup_screen.dart`, `lib/screens/check_in_one_by_one_screen.dart`, `lib/screens/week_review_screen.dart`, `lib/screens/plan_screen.dart`, check-in tests | Optional notes for check-ins remain unchecked | Keep as done |
| Basic progress/stats with 7/30 day summaries, counts, category breakdown, difficulty summary, rhythm, looking ahead | Yes | `lib/screens/progress_screen.dart`, `lib/models/progress_summary.dart`, progress tests | Manual review recommended after real user history accumulates | Keep as done |
| Plain-language privacy/feed explanation | Yes | `lib/screens/settings_screen.dart`, privacy/help tests | Copy may need review before broader launch | Keep as done |
| Firestore persistence for calendars, activities, generated plans, and check-ins | Yes | `lib/state/app_state.dart`, `lib/services/firestore_sync_service.dart`, `lib/services/persistence_service.dart`, Firestore diagnostic | Important nuance: current Firestore state persists activities, seed, settings, locks, and check-ins, then re-derives the generated week. It does not store a full immutable plan snapshot. | Decide later whether seed-based plan persistence is sufficient or whether generated plan snapshots are needed before sharing/export hardening |
| Firestore security rules for shared calendar access | Yes | `firestore.rules`, `tool/diagnostics/check_firebase_rules.ps1`, session log | Needs emulator/rules tests before expanding member roles | Add rules tests when invite/member work starts |
| Local ICS string, feed token metadata, enable/disable, revoke/regenerate, private feed URL, public endpoint behavior | Yes | `lib/services/ics_calendar_service.dart`, `lib/screens/settings_screen.dart`, `netlify/functions/calendar-feed.js`, `netlify/tests/calendar-feed.test.js`, `test/ics_calendar_service_test.dart` | Manually verified in Google Calendar on 2026-06-21: Kwame subscribed to the public feed, the calendar appeared under Other calendars as "Kwame and Laura," and planned Life Shuffle activities rendered on the expected dates/times. Apple Calendar and Outlook were not tested by choice, to move faster for V1. | Keep as done; the Google Calendar smoke test satisfies the V1 real calendar-app subscription test. Revisit Apple Calendar/Outlook later if needed |
| Simple text week export foundation | Yes | `lib/services/text_week_export_service.dart`, `lib/screens/settings_screen.dart`, `test/text_week_export_service_test.dart`, `test/widget_test.dart` Settings export test | Settings currently supports Copy text only. Share sheet and PDF remain unchecked roadmap items. Current app state has no note field; service excludes optional private notes by default. Output-detail toggles (Duration, Category, Check-in status, Locked status, Enabled planning dimensions) are now implemented, see the dedicated row below. | Keep as done for text foundation; build share sheet/PDF later |
| Simple Kwame/Laura membership and selected-calendar access foundation | Yes | `docs/SHARED_CALENDAR_PLAN.md`, `lib/services/firestore_sync_service.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, `lib/widgets/life_shuffle_header.dart`, shared-calendar tests in `test/widget_test.dart` | Manually verified by Kwame on 2026-06-21: shared calendar selection survives reload, member names display as friendly labels instead of raw IDs, calendar rename persists after refresh, and cross-account shared-calendar syncing works. No email invitations, public profiles, create/leave/delete lifecycle, or broader role management yet. | Keep as done; the simple Kwame/Laura membership and selected-calendar access foundation is manually verified. Email invitations, public profiles, calendar create/leave/delete lifecycle, and broader role management remain separate future work |
| Calendar create/select lifecycle slice | Yes | `lib/services/firestore_sync_service.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, lifecycle tests in `test/widget_test.dart` | Settings > Calendar now has a signed-in-only Create calendar row and dialog. Additional calendars use generated Firestore document IDs, owner/member metadata includes the current user, the new calendar becomes selected immediately, and `selectedCalendarId` persists locally. Existing switcher and rename behavior still work after creation. Safe fallback now uses the preserved preferred selection to detect missing/inaccessible calendars, prefers an accessible shared calendar when appropriate, and creates/uses the deterministic personal default with starter/default state when no calendars remain. No leave/delete, feed behavior, or Firestore rules changes were added. | Keep as done for create/select only; member leave and owner delete remain open lifecycle work |
| Calendar member leave lifecycle slice | Yes | `lib/services/firestore_sync_service.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, `firestore.rules`, lifecycle tests in `test/widget_test.dart` | Non-owner members now see Settings > Calendar > Leave calendar for shared calendars. Leave uses a narrow Firestore update that removes only the current user from `memberUserIds` and updates `updatedAtMillis`; it does not delete the calendar, remove other members, or mutate planner/feed/activity fields. After leave, the app reloads accessible calendars and selects another accessible calendar or creates/uses the deterministic personal default with blank starter/default state. Local Firestore rules now include a self-leave allowance, but rules were not deployed. Owner delete, owner removing others, ownership transfer, and feed revocation on delete remain out of scope. | Keep as done for member self-leave only; run/deploy rules only after explicit approval and keep owner delete tracked separately |
| Printable weekly calendar view foundation | Yes | `lib/screens/print_preview_screen.dart`, `lib/services/browser_print.dart`, `lib/screens/settings_screen.dart`, print-preview tests in `test/widget_test.dart` | Read-only print/preview screen for the selected calendar with calendar title, week range, and per-day activity details gated by the same output-detail toggles as the text export (see dedicated row below). Browser print is wired on web via `dart:html` `window.print()` (conditional import; no-ops with a fallback message on non-web/test targets). The back arrow and "Print preview" label are removed from the widget tree for the frame print is triggered, so printed/PDF output starts with the calendar title and week range, not app navigation chrome. Browser Print → Save as PDF is accepted as sufficient for V1; no app-generated PDF export exists. Share-sheet integration is not implemented. | Keep as done for the print-preview foundation; build PDF export and share-sheet integration as separate later work if still wanted |
| Output detail toggles for print/export | Yes | `lib/models/export_print_options.dart`, `lib/services/text_week_export_service.dart`, `lib/screens/settings_screen.dart`, `lib/screens/print_preview_screen.dart`, `lib/state/app_state.dart`, `lib/services/persistence_service.dart`, tests in `test/text_week_export_service_test.dart` and `test/widget_test.dart` | Settings > Export / print has switches for Time, Duration, Category, Check-in status, and Locked status, plus an "Enabled planning dimensions" switch shown only when Difficulty, Energy, or Social is itself enabled in Activity defaults. All default to on, preserving prior output. Applies to both Copy text and Open print view. Activity title and day/date have no toggle and always show. Choices persist locally and sync through the existing flat `SavedState`/Firestore path (no new collection, no rules changes). Private/internal notes are not implemented, so no notes toggle exists. Colour/icon, Location, and "who it is for" toggles are not implemented because those fields do not exist cleanly in the model yet. | Keep as done; add Notes/Location/Who-it's-for toggles only if those underlying fields are added later |
| Calendar owner delete/feed-revocation lifecycle slice | Yes | `lib/services/firestore_sync_service.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, `firestore.rules`, lifecycle tests in `test/widget_test.dart` | Owners now see Settings > Calendar > Delete calendar. The confirmation dialog says "Delete this calendar?", explains "This deletes it for everyone and turns off its calendar feed.", and enables Delete only after the exact calendar name is typed. Delete hard-deletes only the current calendar document; AppState and service guards block non-owners. After delete, the app reloads accessible calendars and selects another one, or creates/uses a blank deterministic personal default without copying deleted calendar contents. Feed behavior is unchanged: because `netlify/functions/calendar-feed.js` looks up by `feedToken` in `calendars`, a hard-deleted document makes the old feed URL return unavailable/404. Local Firestore rules now allow delete only for `ownerUserId == request.auth.uid`; rules were not deployed. | Keep as done for owner hard-delete only; no soft delete, restore, ownership transfer, arbitrary member removal, native PDF, or extra calendar views were added |
| Basic shared-edit/sync conflict messages | Yes | `lib/models/sync_message.dart`, `lib/state/app_state.dart`, `lib/screens/settings_screen.dart`, `lib/screens/plan_screen.dart`, tests in `test/widget_test.dart` | Settings sync diagnostics now shows a calm, plain-language title/body for load, save, member-profile, and permission failures (e.g. "Couldn't save just now. Your changes are still on this device.", "You may not have access to this calendar anymore.") instead of raw Firebase text, plus a Retry button that calls the existing safe `syncWithFirestore()`. The Plan screen shows a small dismissible "Updated elsewhere" notice when a sync detects and applies a newer remote version (only after at least one prior local sync, not on first load). No real-time listeners, no merge UI, no Firestore rules changes. | Keep as done for this basic MVP messaging; richer conflict resolution/merge UI remains future work if needed |

## Current Onboarding Finding

Firestore currently has `displayNameConfirmed: true`, a display name present, `calendarNameConfirmed: true`, `introOnboardingCompleted: true`, and title `Kwame and Laura` for the inspected calendars. Clearing browser/client data does not reset those remote onboarding fields.

The original audit found a mismatch: mini onboarding completion was widget-local only, so reloads showed it again. That is now fixed by persisting `introOnboardingCompleted` locally and in the flat Firestore `SavedState` map. The inspected remote calendars now report `introOnboardingCompleted: true`, so returning signed-in users should skip the intro even after browser/client data is cleared.

Expected behavior after these fixes: when signed in and Firestore data exists, the app shows a small loading state while initial remote sync completes, then skips display-name, calendar-name, and mini intro onboarding if Firestore already confirms them. Settings > Privacy/help includes `Replay intro` for manual replay without resetting the completed state.
