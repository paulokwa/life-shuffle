[build]
  command = "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && ./flutter/bin/flutter build web --release --dart-define=FIREBASE_WEB_API_KEY=$FIREBASE_WEB_API_KEY"
  publish = "build/web"
